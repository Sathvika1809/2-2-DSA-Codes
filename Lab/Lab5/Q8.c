#include <stdio.h>
#include <stdlib.h>
typedef struct Interval{
    int start;
    int end;
}Interval;
int main(){
    int n;
    scanf("%d",&n);
    Interval inter[n];
    for(int i=0;i<n;i++){
        scanf("%d %d",&inter[i].start,&inter[i].end);
    }
    for(int i=0;i<n;i++){
        if(inter[i].end>)
    }



    return 0;
}